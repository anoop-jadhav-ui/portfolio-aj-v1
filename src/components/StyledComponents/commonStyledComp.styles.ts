import styled from "styled-components";

export const SectionTitle = styled.div`
  color: var(--red);

  font-weight: bold;
  font-size: 2rem;

  display: inline;
  position: relative;
`;

export const SubSection = styled.div`
  margin-top: 2.75rem;
  text-transform: uppercase;
  font-size: 1rem;
  color: var(--grey3);
`;

export const SubSectionTitle = styled.div``;

export const SubSectionData = styled.div`
  margin-top: 2rem;
`;
